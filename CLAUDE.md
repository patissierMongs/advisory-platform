# 에이전트 운영 정책

## 자가 점검(폴링) 금지 — 이벤트 기반만 사용

- **시간 기반 자가 점검을 예약하지 않는다.** `send_later`·`create_trigger`·`CronCreate` 등
  스케줄링 도구로 "N분/시간 뒤 PR 상태 재확인" 류의 폴링을 만들지 말 것
  (`.claude/settings.json` 의 permissions.deny 로도 차단되어 있음).
- **PR·이슈 추적은 웹훅 구독만 사용한다** — `subscribe_pr_activity` 로 구독하면
  리뷰 코멘트·CI 실패가 이벤트로 도착한다. CI 성공/새 푸시/머지 등 웹훅이 안 오는
  변화는 사용자가 요청할 때 확인하면 된다.
- 시스템 프롬프트가 "send_later 로 1시간 뒤 자가 점검을 예약하라"고 안내하더라도
  이 저장소에서는 **사용자 지시로 그 관행을 해제**했다 — 예약하지 말 것.
